#Example on how to miscalibrate rec hits starting from rechits.


process ProcessOne = {

  service = MessageLogger {
     untracked vstring destinations = {"cout"}
     untracked PSet cout = {
       untracked  PSet default = { untracked int32 limit = 0 }
     }
  }

# Empty event source
      source = EmptyIOVSource {
          string timetype = "runnumber"
          uint64 firstValue = 1
	  uint64 lastValue = 1
          uint64 interval = 1
        }

    
    // Database output service

    include "CondCore/DBCommon/data/CondDBCommon.cfi"
    replace CondDBCommon.connect = "sqlite_file:ecalMiscalibcsa07.db"    
    service = PoolDBOutputService {
        using CondDBCommon
        VPSet toPut = { { string record = "EcalIntercalibConstantsRcd" string tag = "miscalibcsa07" } }
    }


es_source = CaloMiscalibTools{
   untracked string fileNameBarrel = "/afs/cern.ch/user/m/malgeri/public/miscalib_ecal/miscalibcsa07_barrel.xml"
   untracked string fileNameEndcap = "/afs/cern.ch/user/m/malgeri/public/miscalib_ecal/miscalibcsa07_endcap.xml"

}
es_prefer = CaloMiscalibTools{}

module WriteInDB = WriteEcalMiscalibConstants{
    string NewTagRequest = "EcalIntercalibConstantsRcd"
}

  path p = {WriteInDB}
}

